l1=input("enter a sequence of numbers separated by space")
list=l1.split(" ")
print(len(list))
count=0
for i in list:
    count=count+1
print(count)
for i in range(0,count):
    list[i]=int(list[i])
maximum=list[0]

for every in list:
    if every>max:
        maximum=every
print(maximum)
